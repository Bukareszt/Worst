version: "3.8"
services:
  db:
    image: postgres
      #volumes:
      #- db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: jp2gmd
      POSTGRES_DB: mlm
volumes:
  db-data:
